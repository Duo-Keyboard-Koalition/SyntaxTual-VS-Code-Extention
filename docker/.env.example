# Copy this file to `.env` only if you want to skip the interactive login.

# OPTIONAL: Anthropic API key. If set, Claude Code uses it automatically and
# you can skip the `claude` account-login step.
# Leave blank to log in with your Anthropic account on first run instead.
ANTHROPIC_API_KEY=
